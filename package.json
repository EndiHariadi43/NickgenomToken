{
  "name": "nickgenomtoken",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "clean": "hardhat clean",
    "verify:bsc": "hardhat verify --network bsc $CONTRACT_ADDRESS",
    "verify:bsctest": "hardhat verify --network bscTestnet $CONTRACT_ADDRESS"
  },
  "devDependencies": {
    "hardhat": "^2.22.6",
    "@nomicfoundation/hardhat-verify": "^2.0.8",
    "@openzeppelin/contracts": "5.4.0",
    "dotenv": "^16.4.5"
  }
}
