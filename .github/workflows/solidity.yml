name: Solidity CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18
      - name: Install solc
        run: npm install -g solc
      - name: Compile Contracts
        run: |
          echo "Compiling NickgenomPermit.sol..."
          solc --version
